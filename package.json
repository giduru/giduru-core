{
  "name": "@giduru/ledger-engine",
  "version": "0.1.0",
  "description": "Pure hledger parsing and verification engine extracted from Giduru.",
  "keywords": [
    "giduru",
    "hledger",
    "ledger",
    "parser",
    "accounting"
  ],
  "license": "UNLICENSED",
  "homepage": "https://github.com/giduru/giduru-core#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/giduru/giduru-core.git"
  },
  "bugs": {
    "url": "https://github.com/giduru/giduru-core/issues"
  },
  "publishConfig": {
    "access": "public"
  },
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "require": "./dist/src/index.js"
    }
  },
  "files": [
    "dist/src",
    "README.md",
    "ARCHITECTURE.md"
  ],
  "sideEffects": false,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "bench": "npm run build && node bench/run.js",
    "test": "npm run build && node --test dist/test/**/*.test.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "prepublishOnly": "npm run test"
  },
  "dependencies": {
    "@lezer/common": "^1.5.1",
    "codemirror-lang-hledger": "^0.5.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^5.9.3"
  }
}
